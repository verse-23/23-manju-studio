# 启动与后期交付

## 只问真正缺少的参数

同一作品的追加答案、改参数与增量修改沿用已确认设置。新任务先读取用户所给材料与范围，然后一组选择题最多问2-3项；已确定的跳过。

1. 输入阶段：A 只有剧本，做配乐规划；B 已有配音/粗剪，做选段和实测；C 已有剪映草稿，核对后期执行。
2. 音乐路径：A 现成曲目（优先抖音热门）＋AI原创方案（本用户默认）；B 只找现成曲目；C 只写原创配乐提示词。用户本轮已明确路径时不再问；不再询问是否允许抖音热门或是否参考Apple。
3. 仅在需要实际获取素材时询问获取条件：用户本地库/已购或会员库/公开曲目链接。版权类型不设开关，也不作为本用户必须再确认的问题。

选择题工具不可见时，在正常最终回复里列清楚A/B/C，不能只说“已发出问题”。参考图/音频/草稿文件通过正常附件交接，不用仅支持文字的问答工具索要上传。

用户没给材料时可以先完成通用方法或技能维护，不猜新任务一定使用历史附件。用户给了明确剧本而没有音频，继续做剧本级cue和候选检索；实测是后续升级项，不能把没有音频当作不做选曲的理由。

## 三份关联数据

### 项目基线

作品与源文件、覆盖集数/缺集、交付版本；剪映版本（未知可留未知）；人声“归一目标−23 LUFS、轨道+10 dB”的已确认操作；该链路是否通过导出测量；BGM是否归一；实际参考声轨/混音格式；本次工作级别。

### 曲目台账

`music_id`、曲名、作者/作曲者、准确版本（原版/器乐/stem/演奏版等）、来源URL、查询日期、类型与主题族、结构描述、是否含人声、实测或来源给定的BPM/调性、时长、试听状态、可用选段、获取状态、授权信息状态。

现成音乐另记：`source_platform`、可见的`platform_sound_id`、音源上传者（与作者分开）、`trend_status`、`trend_source_urls`、`trend_checked_at`、`trend_period`、页面真实显示的热度指标及其口径、`jianying_search_terms`、`jianying_availability`。热度状态用“当期榜单已核实/同类近期使用/历史热门/未核实”，含义见[抖音检索](bgm-douyin-discovery.md)。不适用或看不到的数据留空，不编造排名、使用量或剪映曲库ID；AI方案不套热门标签。

一个`music_id`只对应一个确切版本。原版和DJ/加速/器乐版分别编号，cue引用实际版本。提供剪映可复制检索词（曲名＋作者＋版本），实际搜到才标“剪映可用”，否则为“待库内确认”；抖音音源存在不代表剪映有同款。热度证据、音源身份和试听状态各自记录。

版权状态用“官方写明的具体许可”“用户提供，许可未独立核实”“商业发行，授权未核实”等事实描述。不要把“找到官方播放页”“平台曲库内可搜到”当作跨平台发布授权；这是元信息，不是候选过滤或流程拦截。

AI方案使用独立`AI-MUS-xxx`编号，记录正向提示词、排除项、预计结构、预期时长、loop/sting目标与实际生成状态。不能把拟用标题伪装成真实发行曲。

### Cue执行表

建议CSV字段（UTF-8 BOM，中文剪辑团队易导入）：

```text
cue_id,episode,scene,script_anchor,emotion_function,theme_id,music_id,backup_music_id,timing_basis,video_in,video_out,music_in,music_out,duration_status,music_normalization,voice_level_basis,music_gain_db,dialogue_gap_lu,duck_db,fade_in_s,fade_out_s,keyframe_instruction,silence_or_sting,reason,evidence_status,notes
```

- `cue_id`如`EP01-CUE003`。主题在台账统一命名，未补声效时不把BGM短音效当作完整声效包。
- `video_in/out`是影片时间线，`music_in/out`是曲目文件内部时间；二者不能互抄。只凭剧本时用锚点，数字字段留空或明确估计范围，不能填假的00:xx精确数值。
- 帧级时间码必须给fps；没有fps可用秒或`HH:MM:SS.mmm`实测时间，不能混用分秒与帧号。
- `music_gain_db`写实际可执行的条件值或实测建议范围；“音乐比人声低14 LU”不能直接填成“音乐滑块−14 dB”。关键帧注明是相对当前值再降，还是该时刻的绝对增益。
- 留白cue允许`music_id`为空并写“停BGM”；所有非空引用必须对应台账真实条目。
- 有淡入淡出、交叉衔接时，检查选段长度足够；尾音过长不要直接切断或延长视频来凑音乐。

## 页面与检查

页面按集数、主题或音乐编号筛选，突出主选理由、备选差异、非Apple的原始来源链接及试听状态。曲目卡显示具体版本、是否有人声、热度状态/证据/核实日期和剪映检索词，让剪辑师分清“热门”“适配”“已经能用”。可复制AI提示词/剪映关键帧说明，下载cue CSV。无实际音频时不要显示暗示已制作的播放器。候选尚未试听、参数仅为初值、草稿未修改等状态放在相关行，不用统一“已完成”遮掩差别。

HTML和CSV同一份数据导出，中文、编号、引用、行数和版本一致。需要PDF时完整保留曲目、cue及混音条件，使用可用PDF工具生成实际文件并检查文字和分页；链接或打印说明不替代PDF。

只交付本次实际完成的层级。推荐音乐不意味着已经下载、配入草稿或通过审核。默认本地交付，不把剧本、配音或草稿上传公开GitHub。
