# 资产模块参数（统一提问）

先执行总入口的多选菜单，不单独重开一套问卷。默认提供全剧全部必要资产提示词、HTML和完整PDF；用户可指定仅人物或其他类别，仍覆盖本次全剧范围。

本轮尚未明确目标模型时，由总入口合并询问：A ChatGPT Images 2.5 / Sunburst（沿用历史偏好，推荐）；B 即梦；C 通用自然语言提示词。历史型号仅为目标意图，不证明当前工具可调用或原生2K能力。已有本轮答案就沿用。

默认中文3DCG动漫，重视外观、发型和服装剪影的风格化设计，人物提示词不含鞋底；横屏16:9、2560×1440高质量意图；人物板纯白底，左三视图加右五官半身近照。用户另有尺寸或画风时以最新要求为准。资产模块本身只交付提示词，系统UI需要用户选中模块2才批量制作。

新任务必须等待用户的模块选择；用户明确“全部按推荐”才接受已经展示的推荐配置。模块已选但个别参数未答时，可以先完成不依赖该参数的提取，不能假定预选就是答案。技能维护和发布不启动制作问卷。
