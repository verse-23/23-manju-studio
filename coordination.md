# 一次提取，多模块使用

记录源文件名与哈希、实际可读范围、集场及段落定位；需要全文的模块共用同一次完整阅读结果。多个版本先辨别当前版本，不能把两个剧本混成一份事实台账。

共享：角色实体及已揭露身份、出场与关系、造型不变量、场景实体、实际道具、可见系统文字与系统声音、剧情节点。区分剧本事实、已有角色资产/成片证据、艺术设计补全。去重以同一实体和状态为准，不能因名字不同把伪装身份提前公开。

各模块有自己的交付ID，并可引用共享实体ID。例如系统通知SYS-001可关联资产UI-001；人物名牌可引用CHR-001，但文字显示取决于该集观众知情范围。不会为了方便复用，把所有模块硬塞进同一表格或要求不兼容脚本读取同一JSON。

| 所选组合 | 协作方式 |
|---|---|
| 1＋2 | 共享可见UI文字和实体，动态只做一次；资产页关联实际成品，未完成时如实标注 |
| 2＋3 | 可统一色材与字体，保留系统信息和人物/地点介绍各自层级；不同功能不强用同一框 |
| 1＋5 | 封面复用已确认角色外观；资产提示词不等于已有角色图，不声称已经锁脸 |
| 4＋6 | 保留对白时长、留白与节奏标记；后期BGM由6规划，不在视频提示词里重复要求一套音乐 |
| 1＋4＋5 | 人物年龄、发型和服饰连续；封面宣传场景不能拼出剧里不存在的关系或升级状态 |

调度按实际依赖执行：事实提取优先；已有角色资产可立即用于封面；没有依赖的选曲、文案清单等继续推进。无需强制六模块固定流水线，也不能为了“全流程”自动开未选模块。

交付以实际存在的文件为准。总览列出选择范围、全剧覆盖、各模块状态/链接与具体缺项；待参考或待基础稿不能隐去。用户只选1＋6时，完成条件仅为这两项的实际交付，不凭空加视频、封面和成片审核。
