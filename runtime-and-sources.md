# 运行、路径与融合来源

只需安装根目录`23-manju-studio`一个技能；模块入口是带前缀的`*-workflow.md`，不是另六个`SKILL.md`。模块参考、脚本和模板按前缀放在根目录，引用已适配；只有UI元数据位于agents目录。运行脚本时定位其实际路径，把项目文件和输出写在项目工作目录，不要写入技能仓库。

## 依赖按功能使用

- 提取DOCX/TXT/MD、BGM条件增益：Python标准库即可。PDF输入按实际环境可靠提取并保留页码，不把扫描图当成已识别文字。
- 资产HTML＋PDF：Python、Pillow、reportlab及能覆盖中文的本机字体。
- 系统框/名牌：Python、Pillow、FFmpeg（ProRes 4444；可选VP9 Alpha），可选fontTools辅助检查字形。本地编码器可以显式传入`--ffmpeg`或用`FFMPEG_BINARY`；不附带机器绝对路径或编码器二进制。
- 封面机械检查：Pillow；真正的图像生成/编辑使用当前环境实际可调用工具。没有实际工具不能把五套提示词称为五张成图。
- 分镜工具：`storyboard-check_shots.py`；能力范围看本模块参考。BGM工具：`bgm-gain_plan.py`，只算增益，不测音频。

所有命令均以技能根目录为起点，或直接使用脚本的绝对路径。不要把样例里的路径、字体或音源当成已存在素材。技能根目录的`runtime.local.json`（供系统框和名牌共用）是可选本机配置，发布时排除，迁移后按真实环境探测。

## 来源

合并源版本见根目录`module-manifest.json`：资产4.0.0、BGM1.1.0，系统框/名牌/分镜/封面各1.0.0。保留各模块原有来源、内容标准、工具限制与第三方通知；合并后总入口的模块范围与统一提问覆盖原模块的独立启动流程。

封面模块公开包只含融合工作流，不包含用户参考包的第三方图片、原始ZIP或商业字体；其构图策略可不依赖这些图片使用。本地另有合法参考时应先实际查看，不能声称公共包自带了未包含的图。

BGM排除Apple Music/iTunes仅针对音乐参考与来源，不涉及系统框使用的ProRes视频编码。Apple ProRes技术文档可以作为格式说明，不是BGM选曲依据。

公开仓库不附带真实剧本、配音、媒体产物、登录信息或用户机器路径。原六个独立技能可备份归档；安装合并版后使用一个总入口避免重复激活，其他非23系列技能保持独立。
