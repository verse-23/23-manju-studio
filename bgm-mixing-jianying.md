# 剪映音量、归一与对白避让

## 已确认基线与单位

本用户配音：开启响度统一，目标−23 LUFS，音量设置+10 dB。**设置已确认，处理顺序与导出结果未实测。** 不将“−23 dB”机械当成采样峰值，也不把+10 dB当成10%。

- LUFS-I：所选测量区间的综合响度；受素材、门限、时间区间和声道处理影响。
- LU：响度差。计算关系在同一信号经过线性增益、没有额外压缩/削波/自动归一时近似一dB对应一LU。
- dB增益：剪映某段音频的放大/衰减设置，不是该文件的绝对响度。
- dBFS采样峰值与dBTP真峰值不同，低综合响度不保证不削波。

如果确实是先归一−23 LUFS再线性+10 dB，人声综合响度可近似−13 LUFS；这只是条件推导。剪映版本、增强、限幅、归一顺序、剪切或最终导出处理可能改变结果，不能以算式替代测量。CapCut公开说明提到−23 LUFS与关键帧，但不能据此证明本机中文剪映的处理链完全相同。

## 只有剧本时：可试用的起点

下面是本技能的**试混建议，不是平台标准**。前提：人声与BGM都先归一到−23 LUFS，增益确实在归一之后生效，人声+10 dB保持不变。音乐采用拟用片段的响度基准；全曲与选段差异较大时要重测。表中BGM数值是相对其归一基准的**绝对滑块起点**，不是在当前值上再加一次。

| 场景用途 | BGM低于活跃对白的试用差值 | BGM增益起点 |
|---|---|---|
| 密集旁白、设定解释、低声重要信息 | 16-20 LU | −10至−6 dB |
| 普通对白与日常推进 | 12-16 LU | −6至−2 dB |
| 悬疑对白、关键证据 | 14-18 LU | −8至−4 dB |
| 情感对白，配器稀疏 | 10-14 LU | −4至0 dB |
| 动作同时有台词/重要音效 | 14-18 LU作为保守起点 | −8至−4 dB，再听总线 |
| 无对白过桥/情绪释放 | 相比邻近正常人声参考低2-6 LU；不是与静音比较 | +4至+8 dB，先检查峰值 |

对白优先可先试“BGM同样归一−23，再设−6 dB”，听清晰后微调。**未归一的任意原曲不能直接照抄这张表。** 例如商业母带较响，可能需要更大的负增益；安静器乐也可能需要较小衰减。遇到词句不清，先减少编配密度或换选段，再调音量。

如果不能确认BGM归一或处理顺序，只交付响度差目标与“待测后换算滑块”，不要无条件报一个通用−20 dB。也不强制所有素材重新归一；已测原曲可直接计算增益。

## 基于实测换算

同一个拟混对白窗口，分别测导出的最终人声`V`和尚未加本次增益的BGM选段`M`，目标让音乐低`D` LU：

```text
音乐目标响度 = V - D
音乐需施加的线性增益 = (V - D) - M
```

比如人声−13 LUFS、原曲选段−10 LUFS、目标差14 LU，则音乐目标−27 LUFS，BGM应加−17 dB。若BGM已归一−23 LUFS，则同一目标对应−4 dB。两种结果不同但相对目标一致。

测量的是已含当前增益的音乐导出时，公式得到的是**额外调整量**，不能误写为最终剪映滑块。要加回已知当前值，或重新测量未加轨道增益的源文件。条件计算脚本只接受源文件/归一后且尚未加待计算增益的BGM基准，避免混淆。

脚本无需第三方依赖：

```text
python bgm-gain_plan.py normalized --gap-lu 14
python bgm-gain_plan.py measured --voice-lufs -13 --music-lufs -10 --gap-lu 14
python bgm-gain_plan.py measured --voice-lufs -13 --music-lufs -10 --gap-lu 14 --voice-true-peak -2 --music-true-peak -1
```

`normalized`输出条件估计；`measured`表示输入由使用者提供为测量值，脚本本身不检测真假、不读取音频。最后一个命令还计算两条轨道峰值同时同向叠加的保守上界，**不是最终混音真峰值实测**；没有考虑音效或其他轨道，仍要导出混音后检测。源峰值未知则不声称安全。

## 实测和校准流程

选一段实际代表性的正常对白，另含低声与情绪较强的对白。先按用户当前链路分别导出人声和拟用音乐选段，再导出混合片段；所有导出保持相同声道布局与采样率。记录实际处理链、时间范围、I/LRA/TP。短促击点与极短语句不能只看LUFS-I，应同时看峰值、短时/瞬时变化并试听。

有FFmpeg时使用其`ebur128`测量；先检查工具可用。PowerShell形式示例：

```powershell
ffmpeg -hide_banner -nostats -i "dialogue-export.wav" -map 0:a:0 -vn -af "ebur128=peak=true" -f null -
```

读最终Summary，不把处理中途日志的数值当成全段结果。需要限制区间时先准确裁取同一窗口或按实际工具说明设起止；不能比较整首音乐与一小句对白后声称完成精确匹配。原始mono与播放时dual-mono的感知计量有差异，优先测实际导出声道，不随意叠加补偿。

无FFmpeg/响度计/可访问音频时继续交付条件方案，明确待实测；不要用RMS或波形高度冒充LUFS或真峰值。不得为了给数字而虚构工具结果。

不要无条件再次对混音母版做−23归一，导致整个片子被压低。最终平台有明确交付标准时以该标准为准；没有时可以将约−16至−14 LUFS-I作为短片试混参考，真峰值先留到不高于−1.5 dBTP的余量，再按实际试听/转码检查。它们是工作建议，不是红果官方规定。用户既有配音+10不代表允许总线削波；必要时整体留余量并维持对白相对关系。

## 音量关键帧

对白音乐的建议降幅与表中绝对轨道值分开写。例如：无对白段−1 dB，台词期间绝对−7 dB，即额外下降6 dB，不能写成−7后又降7。

1. 台词将开始前约80-150毫秒开始平滑降到对白段电平，让第一个字清楚；突然出现的对白可用更短过渡，以实际听感调整。
2. 同一短句群持续保持较低值；零碎停顿不逐字恢复。
3. 最后一句结束后约300-800毫秒渐回桥段电平，残响和情绪余韵可更慢。
4. 常规音乐入场可试0.3-1秒，退出0.5-1.5秒；悬念切断/击点按剧情处理，先处理波形边界点击声。交叉淡化还要检查低频叠加和乐句冲突。

这些是后期试混参数，不是剪映功能保证。自动ducking/侧链不可用时改用音量关键帧，不编造本机按钮。所谓“对白区降6-12 dB”是从该段无对白基线往下减，不等于所有BGM滑块写−6或−12。

若器乐与人声在中频争抢，优先选更稀疏的版本；可试对音乐约1.5-4 kHz做宽而浅的1-3 dB退让并比较原版，具体频段与量依音色。低频不抢台词也可能掩盖节奏/占用总线余量。耳机、手机外放和单声道各听关键片段，确认对白可辨、音乐不抽动、尾音不被砍掉、混合不削波。
