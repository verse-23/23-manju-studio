# 提取与跨集连续性

## 来源与身份

默认逐集逐场读取所提供的整份剧本，记录作品、源文件版本、实际集场目录、已读范围和缺失部分。不得只分析开头几集便确定全剧母版或交付完成；只有用户明确指定局部任务时才缩小范围。短摘录用于核对，不整段重印剧本。分清“剧本明确”“制作补全”“未知/冲突”，不得把美术假设写成原文。

长剧本允许分段读取，但先汇总全剧人物身份、年龄阶段、服装变化、地点、道具状态与可见UI事件，再编写共享母版和必要变体。跨批次保留稳定ID与覆盖进度，不因分批把同一人物重复建成多个资产；末集新增资产与后段揭示的身份同样纳入。最终交付以实际全剧覆盖为准，不以固定每批集数为完成条件。

提取出镜人物及确需区别的群体；地点及日夜天气变化；关键或反复出现道具；可复用特效；实际可见的系统UI。纯旁白、音乐、只被提及且不需视觉呈现的物件不自动生成资产提示词。

别名有证据才归并。双胞胎、替身、伪装者、克隆和未揭示身份者不能因外观相似合并。年龄阶段属于同一人物不同变体，不能共用同一年龄的外观描述。未知性别/年龄保持未知；可依据明确的学生阶段写适龄外形，不自行补精确岁数。

## 最少而足够的设计

人物锁：脸型、五官结构、发型、肤色、年龄感、身高相对关系、体态、服装剪裁配色。缺少精确外观时按[审美设计](asset-art-direction.md)补足具体骨相、发型和有设计感的服装并标明；主角需协调高颜与固定识别锚点，不能仅做最低限度区分；用户的某一个样例不能套给全剧。

校服按公司标准实质改为符合世界观、有动漫设计感的非校园制服造型，保持身份、时代、剧情关系，说明变化；若已有旧版校服提示词，保留ID并升版本，不直接复制。服装变体由时间和剧情需要触发，不给所有人物机械增加十套衣服。

场景锁：门窗、主要陈设、出入口、主光源、尺度和观察方向。日夜或视角变化不新增地点；同一地点不同角度不能镜像交换门窗。必要空间角度按实际连续性需要提出，不固定强制四机位全套。

道具锁：形状、材质、尺寸、关键标识、归属与状态。拿走、转手、破损、封存按剧情顺序保留，不提前恢复完好。危险/伤害证据按内容标准处理，不凭空改变证据存在与否。

UI锁：模板与显示事件分开，事件包含触发集场、准确文本、数值单位、状态和先后次序。仅有系统声音时不创建可见面板；后续集奖励不提前泄露。FX只提取必要的可复用元素。

## ID、优先级与增量

使用稳定ID，如`WR-CHR-001`、`WR-SCN-001`、`WR-PRP-001`、`WR-FX-001`、`WR-UI-001`。服装/日夜等变体加语义后缀，文件或提示词版本用`v001`递增。已有编号不得因排序变化重新分配。

按首次使用集数、必需性与复用频率排列，先主角/核心场景，再必要变体与附属资产。共用角色母版复用其完整描述，单独新增有叙事依据的差异；输出每条仍完整可复制，不能写“其余同上”。

改版只列新增、沿用、改动和停用提示词及影响集场。角色年龄、服装、场景结构有改动时检查关联变体。源文件不完整时明确覆盖边界，不用固定资产数量填充完成率。

不需要为了完成提示词先生成一张母版图。参考图依赖是下游使用建议，文件必须真实存在才可引用路径；不存在的参考写“尚无，后续使用定稿母版”，不要编造附件ID或假路径。
