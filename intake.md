# 多选菜单与统一问卷

## 新剧本第一步

识别本次实际附件或粘贴文本，检查可读性、作品名与集场目录。没有材料时先提供菜单并说明后续需要剧本，不猜一定使用历史附件。仅收到一个新剧本时，不直接默认制作任何模块。

第一题必须能让用户选择任意组合，题目如下：

```text
这份剧本需要做哪些内容？可多选，回复编号组合（例如1+3+6），也可回复“全部”。
1 资产设计：全剧资产提示词，HTML＋PDF
2 动态系统框：文案提取＋透明MOV视频
3 动态名牌：人物身份＋场景，透明MOV视频
4 分镜提示词优化：Seedance 2.5镜头语言精修
5 漫剧封面：5版不同设计，全部3:4
6 BGM配乐：Incompetech优先＋按需原创方案＋剪映音量
默认范围：本次提供的完整剧本；如只做部分，可在答案里注明。
```

有真正多选控件时使用它；当前只有单选问答工具时，使用**自由文本题**并把上面完整菜单写入题干，不设置单选options来冒充多选。用户回复“1＋3＋6”“资产、名牌、BGM”“除了封面都做”均按明确语义解析；重复项去重。无法对应的编号只澄清该项，不丢失其余明确选择。

若使用`request_user_input_async`，发送一题即可；不要另外发一个版本不同的菜单。没有可用问答工具时，在正常最终回复直接显示菜单等待回答。用户说“题在哪”时重显原题及编号，不生成新题号。不得只回复“已提问”而不提供可见内容。

`全部`等于1–6；它确认模块范围，但不等于同时授权购买、上传业务数据、虚构分镜基础稿或采用尚未收到的参考。明确“本次按默认直接做”可采用已经说明的默认值；沉默、预选和等待时长不算选择。

## 选定之后只补缺项

以本次已选模块建立一张参数表，优先复用当前对话、项目记录和参考。每轮最多2–3个短问题；同一参数只问一次，不把六个模块原问卷全部倒给用户。

| 条件 | 需要的信息 | 可用选项与默认 |
|---|---|---|
| 选1，目标模型本轮未指定 | 提示词目标模型 | Images 2.5 / Sunburst（沿用历史偏好）；即梦；通用。只是目标，能力需核实 |
| 选2或3，没有既定美术方向 | 参考方式 | 按剧本设计并直接制作（推荐）；用户提供参考；沿用已有母版。2和3共享一次回答 |
| 选2，文字版本未知 | 带字或空框 | 带字＋空框（推荐）；只带字；只空框 |
| 选3，名牌范围未知 | 人物、场景及披露 | 默认人物＋场景，按观众当时已知身份；只有明确接受才采用全知介绍 |
| 选4但未给基础分镜 | 基础稿来源 | 提供基础提示词后优化（推荐）；授权先按剧本建立基础分镜再优化 |
| 选5，封面信息不足 | 正式剧名、参考职责 | 从可靠材料确认题名；不能确认则问准确文字。默认5版3:4，题材按剧情判断，沿用现有角色资产 |
| 选6，阶段或路径有缺项 | 只有剧本/已有音频，现成/原创 | 本用户默认Incompetech现成优先、原创按需补缺；已确认现成＋原创时沿用。已有声音文件才做实测，没有音频继续做剧本配乐方案 |

画幅和fps只有存在缺项或冲突时问；16:9项目不覆盖封面3:4规则。实际剪辑工程参数优先；透明视频可先建议30fps，不能把未确认建议写成读取到的工程事实。风格已明确和用户让你自主决定时不重复问参考。

参考图片和音频附件通过正常对话交接，不用文字问答工具索要上传。用户选择自带参考但没有附件时，只暂停依赖参考的设计，继续文案提取及其他已选模块。

## 状态与续做

记录实际显示过的菜单、用户选择、来源版本、范围、各模块参数与状态。至少区分`未选择 / 已选待输入 / 执行中 / 待修复 / 已完成`。只有不依赖未答参数的提取与整理可先做，不用历史答案替代本轮必须确认的新参数。

用户明确授权从剧本起草基础分镜后，4可按本次范围先建立可审阅基础镜头，再用模块4优化；保留事件与对白，不触发额外编剧改写。用户未授权时4待基础稿，其他模块继续。回复“加一个BGM”只是新增6；回复“这版封面换颜色”只处理5的指定版本。

首次新剧本需选择模块；后续续做和上下文压缩不清空已选项。切换到另一剧本时重新显示模块菜单，旧角色事实不迁入新作品，通用个人偏好可作为推荐。
